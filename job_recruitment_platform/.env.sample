SECRET_KEY=dummy-secret-key
DEBUG=True

# Use console backend for development (emails printed in terminal)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Dummy fallback values (not used when console backend is active)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=dummyuser@example.com
EMAIL_HOST_PASSWORD=dummypassword123
DEFAULT_FROM_EMAIL=dummyuser@example.com
